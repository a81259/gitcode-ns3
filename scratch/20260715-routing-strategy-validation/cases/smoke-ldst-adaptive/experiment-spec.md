# Experiment Spec: smoke-ldst-adaptive

## Goal

Validate the pre-registered routing claim for block `smoke-ldst`.

## Topology

- family: `micro-equal`
- transport_channel_mode: `on-demand`
- generated by: `generate_topology.py`

## Workload

- profile: `smoke`
- operation: `MEM_STORE`
- transport: `LDST`

## Routing Intent

- routing_type: `PER_PACKET_SHORTEST_PATHS`
- multipath_selector: `ADAPTIVE`
- path_source: `manual-route-table` for micro topologies, otherwise `auto-path-finder`

## Network Overrides

- flow_control: `CBFC` (runtime catalog default, explicitly pinned)
- congestion_control: disabled
- retransmission: disabled; any drop is a safety failure

## Observability

- tier: `balanced`

## Prediction

LDST completes with the representative legal strategy.

## Falsification Signal

Observed routing behavior contradicts the pre-registered block claim.

## Artifact Contract

The case must contain node, topology, routing, traffic, full network attributes, console log,
runlog traces, and parser summaries. Path and queue metrics are trace-derived.
